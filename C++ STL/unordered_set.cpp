#include <bits\stdc++.h>
using namespace std;
void Unordered_Set()
{
    unordered_set<int> st;
    // Same as normal set but the only difference is that this set can store unique elements in any manner unsorted or sorted.
}
int main()
{
    Unordered_Set();
    return 0;
}