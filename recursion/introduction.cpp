#include<iostream>
#include<utility>
#include<string>
#include<vector>
using std::cout;
using std::cin;
using std::endl;
using std::string;
using std::pair;


int main()
{
    /* 
Recursion is a programming technique where a function calls itself
to solve smaller instances of the same problem. 

A recursive function must have:
1. A base case → the condition to stop recursion.
2. A recursive call → the function calling itself with updated arguments.

Example:
Factorial(n) = n * Factorial(n-1), with base case Factorial(0) = 1
*/

}
